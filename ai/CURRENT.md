# CURRENT.md — Что делать прямо сейчас

**Дата обновления:** 2026-09-07
Полный дизайн и долгосрочное видение: ai/DESIGN.md
## Главная цель
Добить MVP 0.1 до состояния «на примитивах уже весело играть».

## Порядок работ (строго)

0. ~~Плейтест-gate Prompt 13~~ — ПРОЙДЕН в PIE 2026-09-07 (человек реально бежит в Chase, тряска безопасна).
1. ~~Prompt 14 — Минимальная полировка~~ — ГОТОВО 2026-09-07 (цвета блокаута и людей, процедурное аудио без ассетов, читаемый HUD).

**MVP 0.1 ЗАВЕРШЁН (включая финальный QA-этап 2026-09-07).** QA закрыл 6 находок PIE-плейтеста: спавн HUD (HUDClass в ctor GameMode), одноразовый кик камеры при SWAT, трамплин-коллизия комара убрана (ECC_Pawn ignore), покраска доказана readback'ом (19× OK), Lumen-экспозиция починена (cvar в INI). Осталось NEEDS PIE: повторная визуальная проверка владельцем. Следующий этап — спланировать v0.2 вместе с владельцем (кандидаты в ai/DESIGN.md); всё из «Запрещено сейчас» не трогать без явного «ок».

## Что уже работает (не трогать без причины)
- Полёт + камера + Enhanced Input
- Статы комара + тик (голод, энергия, кровь)
- Посадка и укус
- 3 человека с FSM (Calm → Chase) + хлопок
- День/ночь (120 сек сутки)
- Debug HUD (5 баров + состояние человека + время)
- **Chase Score (Prompt 11): таймер погони, очки за выживание, отображение в HUD**
- **Wing Damage & Death (Prompt 12): штрафы скорости от крыльев, Die()/Respawn(), тряска камеры**
- **Balance (Prompt 13): тюнинг 14 констант — комар быстрее, человек слабее, укус выгоднее**
- **Polish (Prompt 14): цвета блокаута и людей (runtime-MID), процедурное аудио (жужжание/хлопок/укус, ноль ассетов), читаемый HUD**
- **Hotfix 2026-09-07: движение человека восстановлено (MOVE_None bug в StopMovementAndClearWander), bRunPhysicsWithNoController=true для NPC**
- **Hotfix 2026-09-07: тряска камеры при Wings<25 ограничена (≤0.4°/кадр, dt≤1/30 с) — не ломается на фризах**
- **QA 2026-09-07: HUD реально спавнится (HUDClass = AMosquitoHUD в ctor GameMode — раньше был пустой AHUD), кик камеры при SWAT (+1.2°/хит, cap 2.0°), комар игнорирует ECC_Pawn (трамплин убран — *временно*), `MosquitoPaint::PaintMesh`: runtime-родительский Unlit-материал с подключённым в граф параметром 'MosquitoColor' + честный probe по РОДИТЕЛЮ (старый readback по MID был ложноположительным — цвета реально видны в PIE), `r.EyeAdaptation.CachedLightingPreExposure=0` (Lumen-ворнинг исчез)**
- **FINAL HOTFIX 2026-09-07: collision Mosquito↔Human восстановлен — `ECC_Pawn` на комаре `ECR_Block` (было `Ignore` → комар проходил сквозь человека). Трамплин не возвращается: комар `Simulate=false`/`MOVE_Flying` (без AddImpulse), человек `bRunPhysicsWithNoController=true` (Kinematic → Slide только геометрия). Диагностический спам `[Human::DIAG]`/`[Human::Chase]` понижен до Verbose. HUD: `Wings → "Wing HP"`. Build Succeeded, headless чисто (0 fatal/ensure/crash, 19× readback=OK, игрок/tick работает).**
- **Collision hotfix v3 2026-09-07 (новый агент): схема заменена на `ECC_Pawn=ECR_Overlap` + ручная коррекция.** Human не воспринимает комара как препятствие вообще (0 impulse/Launch/Z-реакций, Wander/Chase не прерываются). Комар не может находиться внутри человека: `AMosquitoCharacter::ResolvePawnPenetration()` в Tick (только в полёте) делает point-sweep капсулы из центра человека и двигает ТОЛЬКО комара (`SetActorLocation`, скорость не тронута). Посадка/укус не задеты (коррекция выключена при `bIsLanded`). Лог Verbose. Build Succeeded, headless ×2 чисто (EXIT=0, 0 fatal/ensure/наших Warning). Ждёт ручной PIE-чек владельцем. Подробнее: ai/HANDOFF.md.

## Запрещено сейчас

- Пауки, поколения, генетика, посёлок, расписания, погода, настоящий UI, сейвы.